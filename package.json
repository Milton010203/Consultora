{
  "name": "consultora",
  "version": "1.0.0",
  "scripts": {
    "watch-css": "node_modules/.bin/sass --watch scss:css --style=expanded",
    "build-css": "node_modules/.bin/sass scss:css --style=expanded",
    "build": "npm run build-css",
    "start": "gunicorn app:app"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}

